# Local development — use local API
NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Google OAuth (server-only — do not prefix with NEXT_PUBLIC_)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Must match Authorized redirect URI in Google Cloud Console
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# Web3Forms — public client-side access key for /contact page
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=9950af42-925b-444e-9087-4c4367d48650

# Google Analytics 4 measurement ID
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-EGX758D2QW
